hello = "helloooo baby !!!"
IO.puts(hello)

hello_multiple_line = """
  A multiline
  hello world
  greeting !
"""

IO.puts(hello_multiple_line)

list = [1, 0, "sushant", :sahu, true]
IO.inspect(list)

tuple = {1, "sushant", :sahu, 2}
IO.inspect(tuple)
